services:
  broker:
    image: docker.io/library/redis:8
    container_name: paperflow-redis
    restart: unless-stopped
    volumes:
      - redisdata:/data
  db:
    image: docker.io/library/postgres:17
    container_name: paperflow-db
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: ${PAPERLESS_DB_PASSWORD}
  webserver:
    build:
      context: .
      dockerfile: Dockerfile
    image: paperflow-ai:local
    container_name: paperflow
    restart: unless-stopped
    depends_on:
      - db
      - broker
    ports:
      - "8010:8000"
    volumes:
      - data:/usr/src/paperless/data
      - media:/usr/src/paperless/media
      - ./export:/usr/src/paperless/export
      - ./consume:/usr/src/paperless/consume
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: db
      PAPERLESS_URL: ${PAPERLESS_URL:-http://localhost:8010}
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY}
      PAPERLESS_TIME_ZONE: ${PAPERLESS_TIME_ZONE:-UTC}
      PAPERLESS_OCR_LANGUAGE: ${PAPERLESS_OCR_LANGUAGE:-eng}
      PAPERLESS_MISTRAL_API_KEY: ${PAPERLESS_MISTRAL_API_KEY}
      PAPERLESS_OPENAI_API_KEY: ${PAPERLESS_OPENAI_API_KEY:-}
      PAPERLESS_CHAT_MODEL_NAME: ${PAPERLESS_CHAT_MODEL_NAME:-mistral-large-latest}
      PAPERLESS_EMBEDDING_MODEL: ${PAPERLESS_EMBEDDING_MODEL:-mistral-embed}
      PAPERLESS_REDIS_EMBEDDING_URL: ${PAPERLESS_REDIS_EMBEDDING_URL:-redis://broker:6379}
      PAPERLESS_EMBEDDING_ENABLED: ${PAPERLESS_EMBEDDING_ENABLED:-true}
      USERMAP_UID: ${USERMAP_UID:-1000}
      USERMAP_GID: ${USERMAP_GID:-1000}
volumes:
  data:
  media:
  pgdata:
  redisdata:
